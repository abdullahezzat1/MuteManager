import currentBrowser from "/modules/m1.js";

// currentBrowser.tabs.query({}, function (tabs, errors) {
//   tabs.forEach(function (tab) {
//     currentBrowser.tabs.update(tab.tabId, { muted: true })
//   })
//   console.log(tabs);
// });
// currentBrowser.tabs.query({}).then(logTabs, onError);
